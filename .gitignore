# secrets
.env
.env.*
!.env.example

# dependencies
node_modules/
web/node_modules/

# next.js
web/.next/
# stray root builds (running next from the repo root by mistake)
/.next/
web/out/
web/build/
web/next-env.d.ts
web/*.tsbuildinfo

# vercel
web/.vercel

# testing
web/coverage/

# certs / keys
*.pem

# self-hosted media (large; lives in R2 in prod) + dev symlink.
# Anchored to the ROOT folder only: an unanchored media/ also swallowed the
# web/app/api/*/media API route directories (the crop proxy never reached
# git, so prod deployed without it).
/media/
web/public/media

# scraper exports / raw data (DB is source of truth)
main-table.json
main-table.csv
nepali-restaurants-au.json
nepali-restaurants-au.csv

# agent skills (local tooling, re-fetch via `npx skills add`; skills-lock.json is committed)
.agents/
.claude/skills/

# os / editor / logs
.DS_Store
*.log

# Neon/Postgres dumps (data snapshots)
backups/
scraper/menu-data/.claims/

# menu-fetch.js scratch (downloaded PDFs / rasterized pages) + ad-hoc scratch scripts
scraper/.menu-tmp/
scraper/_tmp*.mjs
