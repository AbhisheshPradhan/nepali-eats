Cuisine / attribute chip — also works as a clickable filter chip.

```jsx
<Tag>Momo</Tag>
<Tag clickable active>Newari</Tag>
<Tag clickable onClick={...}>Veg-friendly</Tag>
```

`active` fills it chili-red; `clickable` adds hover affordance. Use for cuisine types, dietary tags, and the filter bar.
