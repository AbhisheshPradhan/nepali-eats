Pill-shaped text input with optional leading icon; marigold focus ring.

```jsx
<Input placeholder="Search momo, thali, suburb…" iconLeft={<i className="ph ph-magnifying-glass" />} />
<Input size="lg" placeholder="Your suburb" />
```
